# Day 23 article workflow record

This developer-facing note records the required writing workflow for the
reader-facing article `docs/day23-onnx-runtime-inference.md`.

- The `technical-blog-writer` skill was read before drafting the article.
- Its article-structure, writing-style, visualization-policy,
  mermaid-workflow, and review-checklist references were read before the
  corresponding writing and figure actions.
- The article promise was fixed as: explain why a successful ONNX export is
  insufficient, then use real PyTorch/ORT Q-value and action comparisons to
  decide whether the runtime swap preserved the policy decision.
- The parity plot was generated from the saved Day 23 comparison artifact, and
  the flow diagram was authored from the verified implementation sequence,
  rendered to PNG, and visually inspected before embedding.
